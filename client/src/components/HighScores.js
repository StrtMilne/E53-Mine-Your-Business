import { ReactFragment } from "react";

const HighScores = ({highScores}) => {

    // const highScore_nodes = highScores.map((score, index) => {
    //     return (
    //         <HighScoreItem key={index} score={score.score} name={score.player_name} />
    //     )
    // })

    return(
        <ol>
            {/* {highScore_nodes} */}
        </ol>
    )

}

export default HighScores;
